# Binaries
/guardiand
/guardian-train
/guardian-loadtest
/bin/
/dist/

# Test & coverage
*.test
coverage.out

# `make bench-report` raw run (a machine-specific snapshot, never a baseline
# to commit; the committed baselines live in allocs-baseline.txt)
/bench-current.txt

# Runtime data
*.db
*.key
*.rotate.lock
*.crt

# GeoLite databases (except committed test/demo fixtures)
*.mmdb
!core/intel/testdata/*.mmdb
!test/seed/seed-*.mmdb

# Editor / OS
.idea/
.vscode/
*~

# Claude Code agent worktrees (parallel sessions; never commit these)
.claude/worktrees/
.hermes

