# Harness-only credentials for the auth_basic location in angie.docker.conf.
# {PLAIN} is an Angie/nginx-supported scheme; this file never leaves the
# compose network and guards a fixture path, not anything real.
e2e:{PLAIN}s3cret
